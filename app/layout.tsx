import type { Metadata, Viewport } from 'next'
import { ClerkProvider } from '@clerk/nextjs'
import './globals.css'

const siteUrl = process.env.NEXT_PUBLIC_SITE_URL
  ?? (process.env.VERCEL_URL ? `https://${process.env.VERCEL_URL}` : 'https://quickyield.vercel.app')

export const metadata: Metadata = {
  metadataBase: new URL(siteUrl),
  title: 'QuickYield - DeFi Yield Radar',
  description:
    'QuickYield scans live DeFi yield pools with safety context, target rules, and Telegram alerts for non-custodial yield research.',
  icons: {
    icon: '/favicon.svg',
  },
  openGraph: {
    title: 'QuickYield - DeFi Yield Radar',
    description:
      'Scan DeFi yield pools, save targets, and get alerts when cleaner opportunities match your rules.',
    type: 'website',
    siteName: 'QuickYield',
    // Social card image is generated by app/opengraph-image.tsx (1200×630 PNG).
  },
  twitter: {
    card: 'summary_large_image',
    title: 'QuickYield - DeFi Yield Radar',
    description:
      'The Bloomberg Terminal for DeFi yield — scan live pools, grade their safety A–F, and get alerted before yields turn.',
  },
}

export const viewport: Viewport = {
  themeColor: '#0a0d12',
  colorScheme: 'dark',
}

export default function RootLayout({ children }: Readonly<{ children: React.ReactNode }>) {
  return (
    <ClerkProvider>
      <html lang="en">
        <body>{children}</body>
      </html>
    </ClerkProvider>
  )
}
